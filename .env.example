# Environment Variables Template
# Salin file ini menjadi .env dan sesuaikan nilainya

# Port untuk local server (default: 3000)
PORT=3000

# Node environment (development | production)
NODE_ENV=development

# CORS origin (default: * untuk semua origin)
# Contoh: CORS_ORIGIN=https://myapp.com
CORS_ORIGIN=*

# ============================================
# CATATAN:
# ============================================
# - File .env TIDAK boleh di-commit ke Git
# - Tambahkan .env ke .gitignore
# - Untuk Vercel, set environment variables di dashboard Vercel
